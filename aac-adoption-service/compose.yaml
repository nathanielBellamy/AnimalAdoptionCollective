services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=aac-adoption-service'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432'
